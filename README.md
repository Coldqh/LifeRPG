# PRIME RPG v0.8

Личная RPG-система прокачки реальной жизни.

Статическое приложение: `HTML + CSS + JavaScript`. Сервер не нужен. Данные хранятся в браузере через `localStorage`.

## Что работает

- live-день по Москве;
- автообновление дня в 00:00 МСК;
- автообновление недели в ночь с воскресенья на понедельник;
- ежедневные квесты;
- недельная вкладка;
- штрафы;
- pending XP до закрытия дня/недели;
- XP и ранги дня;
- общий уровень;
- статы персонажа;
- история дней и недель;
- полный сброс истории;
- экспорт/импорт backup JSON;
- импорт кастомных квестов через quest pack JSON;
- максимум 10 квестов на категорию;
- PWA/offline-cache.

## Быстрый запуск локально

```bash
python -m http.server 8080
```

Открыть:

```text
http://localhost:8080
```

## GitHub Pages

1. Загрузи файлы в корень репозитория.
2. Открой `Settings → Pages`.
3. Source: `Deploy from a branch`.
4. Branch: `main`.
5. Folder: `/root`.

Ссылка будет вида:

```text
https://coldqh.github.io/LifeRPG/
```

После обновления открывай с версией:

```text
https://coldqh.github.io/LifeRPG/?v=0.8.0
```

## Quest pack

В настройках можно скачать шаблон задач, отправить его в ChatGPT, заполнить и импортировать обратно.

Минимальный формат:

```json
{
  "type": "prime-rpg-quest-pack",
  "mode": "merge",
  "dailyQuests": [
    {
      "category": "BODY",
      "title": "BODY",
      "stat": "BODY",
      "items": [
        { "text": "мобилити 15 минут", "xp": 10, "stat": "BODY" }
      ]
    }
  ]
}
```

`mode: merge` добавляет квесты.  
`mode: replace` заменяет квесты в указанной категории.

Поддерживаемые статы:

```text
BODY, FIGHTER, MIND, WORK, CREATOR, CALM, DISCIPLINE
```

## Важно

Данные лежат локально в браузере. Раз в неделю делай экспорт backup JSON.

## v0.8

- Вкладка `Пульт` переименована в `Профиль`.
- Вкладка `Боссы` переименована в `Неделя`.
- Убраны SOCIAL/CHARISMA и MONEY из ежедневных квестов.
- Убраны SOCIAL и MONEY из недельных категорий.
- Из недельных категорий убрано слово `BOSS`.
- Убран блок `Season 1` из профиля.
- Убрано описание недельной вкладки.
- XP больше не применяется сразу после галочки.
- Текущие галочки считаются как pending: `текущий XP + будущий XP`.
- Статы показывают формат вида `15+40 XP`.
- Реальный XP засчитывается только после закрытия дня/недели.
